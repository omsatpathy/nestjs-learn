export type CreateUserParams = {
    email: string;
    password: string;
    first_name: string;
    last_name: string;
    dob: Date;
};


export type authParams = {
    email: string;
    password: string;
}