export type CreateUserParams = {
    email: string;
    name: string;
    phone_no: number;
    occupation: string;
    address: string;
    password: string;
};
export type authParams = {
    email: string;
    password: string;
};
